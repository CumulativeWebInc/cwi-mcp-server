# Vendored CWI Verdict Engine

- **Engine:** cwi-verdict-engine v1.0.0 (spec v1.0.0)
- **Source:** `CumulativeWebInc/cwi-learn`, branch `needs/verdict-engine`
  (`trust/engine.py`), fetched 2026-09-17
- **Integrity:** byte-identical vendored copy — weights, thresholds, and
  gates are spec-versioned and MUST NOT be edited ad hoc (trust-verdict
  skill operating rule 4). Spec bumps live in the cwi-learn repo.
- **License/terms:** engine and spec are public per the skill; all
  computation is local, no auth.

To refresh: re-fetch `trust/engine.py` from the branch above and diff.
